import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#323635"
        },
        secondary: {
            main: "#f2eeed"
        }
    },
});

export default theme;