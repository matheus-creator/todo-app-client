import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#323635"
        },
        secondary: {
            main: "#f2eeed"
        },
        alternative: {
            main: "#6f7475"
        }
    },
});

export default theme;